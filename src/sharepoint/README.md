# SharePoint (Backend)

Enthält die Definitionen des SharePoint-Backends, das die Java-Anwendung über die Microsoft Graph API bereitstellt und befüllt.

## Unterordner
- `templates/` – SharePoint Site Templates (.xml)
- `listen/` – SharePoint Listen-Schemas (JSON)
